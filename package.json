{
  "name": "streamrush",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "pnpm --filter @workspace/stream-rush run build && pnpm --filter @workspace/api-server run build",
    "start": "node --enable-source-maps api-server/dist/index.mjs",
    "db:push": "pnpm --filter @workspace/db run push"
  }
}
