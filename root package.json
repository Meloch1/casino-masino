{
  "name": "streamrush",
  "version": "1.0.0",
  "private": true,
  "packageManager": "pnpm@9.0.0",
  "scripts": {
    "build": "pnpm -r run build",
    "db:push": "pnpm --filter lib-db run db:push",
    "start": "pnpm --filter api-server run start"
  }
}
